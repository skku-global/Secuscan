# PYTHON-SPECIFIC: an __init__.py file marks this folder as a "package", which
# is what lets `from scanning.engine import run_scan` work from main.py.
# JavaScript has no equivalent — in JS a folder is importable just by existing.
# The file is intentionally empty; its presence is the whole point.
